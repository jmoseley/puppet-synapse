# This file maintained by Puppet!
# synapse

description	"synapse"

respawn
respawn limit 10 5

# Backwards compatibility to run as the HAProxy user to give it permission to reload and stuff
synapse --config <%= @config_file %>
